## 1. Документация схемы БД (ветка PR #20)

- [x] 1.1 Убрать сущность `TASK_ASSIGNEE` и связи `TASK ||--o{ TASK_ASSIGNEE` и `USER ||--o{ TASK_ASSIGNEE` из ER-диаграммы в `docs/database-schema.md`.
- [x] 1.2 Переименовать поле `created_by` в `owner_id` в блоке `TASK` на ER-диаграмме.
- [x] 1.3 Удалить раздел с описанием сущности `TASK_ASSIGNEE` и её таблицу полей.
- [x] 1.4 Обновить раздел «Сущности» для `TASK`: один владелец `owner_id`, он же исполнитель; убрать упоминание нескольких исполнителей.
- [x] 1.5 Заменить обоснование nullable `page_id`: задача может быть личной и не относиться к документу, вместо прежней формулировки про доступ исполнителя к странице.
- [x] 1.6 Сверено: отдельного раздела «Связи» в `docs/database-schema.md` нет — связи выражены только ER-диаграммой (задачи 1.1 и 1.2). Текстовый раздел «Связи» существует лишь в теле issue #9 и правится задачей 3.1.
- [x] 1.7 Добавить в документ таблицу различий между сущностью `TASK` и блоком типа `todo` согласно решению 4 в `design.md`.
- [x] 1.8 Обновить раздел «Ключевые решения»: заменить пункт про nullable `page_id` и добавить пункт про единственного владельца задачи.
- [x] 1.9 Отрендерить обе mermaid-диаграммы через `@mermaid-js/mermaid-cli` и убедиться, что они собираются без ошибок и остаются читаемыми.

## 2. План MVP (ветка PR #22)

- [ ] 2.1 Обновить этап 4 в `docs/mvp-plan.md`: убрать `TASK_ASSIGNEE` из перечня таблиц, заменить «несколько исполнителей» на одного владельца.
- [ ] 2.2 Обновить строку про состав MVP в разделе «Что входит в MVP»: убрать несколько исполнителей у задач.
- [ ] 2.3 Обновить сводку недостающих работ: убрать упоминание исполнителей из состава API задач.

## 3. Синхронизация задач на GitHub

- [ ] 3.1 Обновить тело issue #9: привести ER-диаграмму и описание сущностей в соответствие с новой моделью.
- [ ] 3.2 Обновить тело issue #17: убрать из requirements назначение нескольких исполнителей и аватары исполнителей, убрать соответствующий пункт из acceptance criteria, поправить упоминание `TASK_ASSIGNEE` в разделе Context.

## 4. Проверка и завершение

- [ ] 4.1 Проверить, что ни в одном изменённом документе и issue не осталось упоминаний `TASK_ASSIGNEE`, нескольких исполнителей и поля `created_by`.
- [ ] 4.2 Выполнить `openspec validate simplify-task-ownership --strict`.
- [ ] 4.3 Выполнить `pnpm lint` и убедиться, что проверка проходит.
- [ ] 4.4 Получить human review артефактов и итоговых правок.
- [ ] 4.5 Заархивировать change в том же PR, где находятся правки документации схемы.
