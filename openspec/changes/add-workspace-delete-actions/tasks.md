## 1. API exports, route contract and page-tree helpers

- [x] 1.1 Экспортировать из `apps/web/src/shared/api/index.ts` существующие generated operations для soft delete: `useDeletePage`, `deletePage` mutation options при необходимости, `useDeleteProject`, `deleteProject` mutation options при необходимости; проверка — `pnpm --filter @lite-notion/web typecheck` видит imports без deep import из `shared/api/generated`.
- [x] 1.2 Перенести минимальный workspace route contract из `pages/workspace` в допустимый нижний слой, предпочтительно `src/shared/routing`: `{ type: 'root' }`, `{ type: 'project'; projectId }`, `{ type: 'page'; pageId }` и helpers root/project/page paths; проверка — `features/workspace-management` не импортирует `WorkspaceRouteContext` из `pages`.
- [x] 1.3 Ввести тонкий persistent workspace route-group layout для workspace routes (`/`, `/projects/[projectId]`, `/pages/[pageId]`) без изменения URL и разместить в нём workspace-local delete cleanup coordinator/provider; проверка — provider переживает переходы root/project/page, а `profile` и auth routes не становятся частью workspace lifecycle.
- [x] 1.4 Обновить touched create/select/navigation call sites на route helpers без глобального routing refactor; проверка — существующие route/navigation тесты продолжают ожидать те же URL.
- [x] 1.5 Добавить pure helpers в entity page tree model: удаление page subtree из `PageTreeNodeDto[]`, удаление всех страниц проекта из cached tree и проверка принадлежности page id к subtree через `NormalizedPageTree`; проверка — unit tests покрывают root deletion, nested deletion, unrelated sibling preservation и project filtering.
- [x] 1.6 Проверить idempotency cache cleanup helpers: повторное удаление уже отсутствующего subtree/project не меняет unrelated data и не приводит к ошибке; проверка — unit tests вызывают helpers повторно для page subtree и project filtering.
- [x] 1.7 Убедиться, что helpers не создают отдельную копию entity-модели и используют существующие `NormalizedPageTree`/`childIdsByParentId`; проверка — код не вводит новый state manager и не дублирует tree normalization.

## 2. Feature-level delete orchestration

- [x] 2.1 Расширить `usePageManagement(routeContext)` методом `deletePage(pageId)` на базе `useDeletePage`: до mutation определить delete context и redirect target из текущего page tree cache, после success для affected route вызвать `router.replace(workspaceProjectPath(projectId))` и передать immutable pending cleanup record в persistent coordinator, а локально удалить subtree из `getGetPageTreeQueryKey()` и запустить targeted invalidation только после safe route lifecycle point; unrelated page delete чистит cache сразу после success. Проверка — model/workspace mutation tests покрывают active page, ancestor, delayed route transition, unmount/remount lifecycle и unrelated deletion.
- [x] 2.2 Добавить feature-level `useProjectDeletion(routeContext)` с методом `deleteProject(projectId)` на базе `useDeleteProject`: до mutation определить, затронут ли route, после success для affected route вызвать `router.replace(workspaceRootPath())` и передать immutable pending cleanup record в persistent coordinator, а удаление project из `getListProjectsQueryKey()`, страниц проекта из `getGetPageTreeQueryKey()` и targeted invalidation выполнить только после safe route lifecycle point; unrelated project delete чистит cache сразу после success. Проверка — tests покрывают current project, current page project, delayed route transition, unmount/remount lifecycle и other project deletion.
- [x] 2.3 Обработать pending/error без optimistic removal: duplicate submit невозможен, при API error cache и route не меняются, ошибка пробрасывается в dialog, после error pending снимается и dialog снова можно закрыть или повторить; проверка — tests подтверждают один DELETE request при двойном confirm и отсутствие преждевременного исчезновения resource.
- [x] 2.4 Не импортировать generated mutation hooks из `widgets`, `pages` или tree/item UI components напрямую; проверка — architecture/import search показывает API mutation ownership в `features/workspace-management`.
- [x] 2.5 Реализовать workspace-local cleanup coordinator так, чтобы route-scoped feature hooks не владели lifecycle persistence: `WorkspacePage` сообщает текущий route context coordinator, affected delete hooks только планируют cleanup record; проверка — feature не импортирует `pages`, layout/provider не владеет generated DELETE mutation, pending record содержит только минимальные immutable данные.
- [x] 2.6 Не заставлять tree/item components парсить pathname или владеть route decisions: текущий route context определяет `WorkspacePage`/composition layer и передаёт в feature hooks/coordinator; проверка — search не находит `usePathname` или route parsing в delete UI.

## 3. Confirmation UI and page delete actions

- [x] 3.1 Добавить domain confirmation dialog в `features/workspace-management/ui`, переиспользующий `Dialog`, `Button`, `Text` и destructive variants; проверка — dialog показывает тексты `Удалить страницу?`/`Удалить проект?`, `Отмена`, `Удалить`, pending label `Удаляем…` и error с `role="alert"`.
- [x] 3.2 Реализовать pending close semantics: до confirm `Отмена`, Escape, close control и outside/overlay interaction закрывают dialog без DELETE и возвращают focus; во время pending повторный submit невозможен, destructive button disabled, Cancel/Escape/close control/outside/overlay не закрывают dialog; после error dialog остаётся открыт, pending снят, close/retry снова доступны; проверка — RTL tests покрывают все три состояния.
- [x] 3.3 Подключить page delete intent к основному `PageTree`: menu item `Удалить` открывает confirmation dialog, сохраняет return focus и вызывает общий feature callback только после confirm; проверка — `page-tree.spec.tsx` покрывает presence, open, cancel, confirm callback и focus return.
- [x] 3.4 Подключить тот же page delete flow к `WorkspaceTree`: page menu содержит `Удалить`, использует общий callback и не дублирует deletion business logic; проверка — `workspace-tree.spec.tsx` покрывает sidebar action без повторения всех model-level сценариев.
- [x] 3.5 Проверить, что page confirmation содержит актуальный title выбранной страницы и обновляется при последовательном открытии dialog для разных страниц; проверка — tests ожидают текст `Страница «{title}» и все вложенные страницы будут перемещены в корзину.` именно для выбранного item.
- [x] 3.6 Использовать `lucide-react` для новых icon-only triggers/destructive affordances там, где иконка уместнее текста; проверка — icon-only buttons имеют русские `aria-label`, icons помечены `aria-hidden`, visible destructive menu item text остаётся явным.

## 4. Project delete actions in workspace UI

- [x] 4.1 Расширить project item в `WorkspaceNavigation` action surface: создание root page остаётся доступным, project delete доступен как `Удалить проект` через semantic button/menu item; проверка — keyboard и click tests не ломают navigation по project item.
- [x] 4.2 Добавить project delete action на root workspace project cards, не превращая delete control в часть navigation link; проверка — click по card продолжает открывать project, click по delete action открывает confirmation и не вызывает navigation.
- [x] 4.3 Подключить оба project delete entry points к одному feature-level delete dialog и callback; проверка — tests подтверждают одинаковое confirmation/error/pending поведение для navigation и root cards без независимой реализации.
- [x] 4.4 Проверить, что project confirmation содержит актуальное имя выбранного project и обновляется при последовательном открытии dialog для разных проектов; проверка — tests ожидают текст `Проект «{name}» и все его страницы будут перемещены в корзину.` именно для выбранного project.

## 5. Cache, navigation and error tests

- [x] 5.1 Добавить MSW tests для page delete: DELETE не вызывается до confirm, Cancel не вызывает DELETE, Confirm передаёт правильный `pageId`, duplicate submit невозможен, success удаляет subtree без reload, API error оставляет resource и route; проверка — `pnpm --filter @lite-notion/web test -- workspace`.
- [x] 5.2 Добавить routing tests для page delete: удаление текущей страницы вызывает `router.replace` на project root, удаление ancestor текущей страницы вызывает `router.replace` на project root, удаление unrelated страницы не меняет route; проверка — `expect(router.replace).toHaveBeenCalledWith(expectedProjectRoot)` и `expect(router.push).not.toHaveBeenCalled()`, без попыток эмулировать очистку всего browser history.
- [x] 5.3 Добавить acceptance tests с задержанной route transition: пользователь остаётся на `/pages/child` один render после successful active page delete или ancestor delete, `router.replace(projectRoot)` уже вызван, local cache ещё содержит данные старого route, экран `Ничего не найдено` не появляется; после rerender с project route выполняется subtree cache cleanup и удалённые страницы исчезают из UI.
- [x] 5.4 Добавить test, что navigation после affected page delete не блокируется ожиданием `invalidateQueries`/refetch; проверка — delayed refetch не мешает немедленному `router.replace`.
- [x] 5.5 Добавить lifecycle acceptance test для affected page delete с controlled unmount/remount: после successful active/ancestor delete старый owner размонтируется, новый workspace route owner монтируется, pending cleanup record не теряется, deleted subtree удаляется из cache, unrelated pages сохраняются, `Ничего не найдено` не появляется во время старого route, cleanup helpers/invalidation не выполняются больше необходимого; проверка — harness тестирует observable guarantee, а не внутренний `setState`.
- [x] 5.6 Добавить MSW tests для project delete: action доступен, confirmation открывается, Cancel не вызывает DELETE, Confirm передаёт правильный `projectId`, duplicate submit невозможен, projects cache и page-tree cache обновляются, API error не удаляет project из UI; проверка — root cards и navigation covered.
- [x] 5.7 Добавить routing tests для project delete: удаление текущего project route и project текущей page вызывает `router.replace('/')`, удаление другого project не меняет route; проверка — `expect(router.replace).toHaveBeenCalledWith('/')` только в затронутых сценариях и `expect(router.push).not.toHaveBeenCalled()`, без попыток эмулировать очистку всего browser history.
- [x] 5.8 Добавить acceptance tests с задержанной route transition: пользователь остаётся на route удаляемого project или странице project один render после successful project delete, `router.replace('/')` уже вызван, local cache ещё содержит данные старого route, экран `Ничего не найдено` не появляется; после rerender с workspace root выполняется projects/page-tree cache cleanup и удалённый project/pages исчезают из UI.
- [x] 5.9 Добавить test, что navigation после affected project delete не блокируется ожиданием `invalidateQueries`/refetch; проверка — delayed projects/page-tree refetch не мешает немедленному `router.replace`.
- [x] 5.10 Добавить lifecycle acceptance test для affected project delete с controlled unmount/remount: после successful delete текущего project/page старый owner размонтируется, новый workspace root owner монтируется, pending cleanup record не теряется, project удаляется из projects cache, страницы project удаляются из page-tree cache, unrelated projects/pages сохраняются, unavailable flash отсутствует; проверка — harness тестирует observable guarantee и idempotent cleanup.
- [x] 5.11 Проверить accessibility destructive flow: action menu доступен с клавиатуры, confirmation dialog закрывается Escape/Cancel/outside/close без DELETE до confirm, focus возвращается, pending Escape/Cancel/outside/close не закрывают dialog и DELETE остаётся вызван ровно один раз, error имеет `role="alert"` и после error close/retry снова доступны; проверка — React Testing Library queries по roles/names.

## 6. Final checks

- [x] 6.1 Выполнить `pnpm lint`; проверка — команда завершается успешно.
- [x] 6.2 Выполнить `pnpm --filter @lite-notion/web typecheck`; проверка — команда завершается успешно.
- [x] 6.3 Выполнить `pnpm --filter @lite-notion/web test`; проверка — команда завершается успешно.
- [x] 6.4 Выполнить `pnpm --filter @lite-notion/web build`; проверка — команда завершается успешно.
- [x] 6.5 Выполнить `openspec validate add-workspace-delete-actions --strict`; проверка — команда завершается успешно перед объявлением change готовым к implementation review.

## 7. Review fixes

- [x] 7.1 Для affected page delete отменять in-flight `getGetPageTreeQueryKey()` перед DELETE, чтобы refetch не мог убрать active page из cache до commit нового route; проверка — regression test запускает delayed refetch, подтверждает active page delete, завершает refetch данными без deleted page и не видит `Ничего не найдено`.
- [x] 7.2 Для affected project delete отменять in-flight `getGetPageTreeQueryKey()` и `getListProjectsQueryKey()` перед DELETE, чтобы refetch не мог убрать active project/page из cache до commit нового route; проверка — regression test запускает delayed projects/page-tree refetch, подтверждает affected project delete, завершает refetch данными без deleted project/pages и не видит `Ничего не найдено`.
- [x] 7.3 Сделать `DeleteConfirmationDialog` устойчивым к очистке intent при закрытии: final focus берётся из последнего сохранённого `returnFocus`; проверка — Cancel и Escape реально закрывают controlled dialog и возвращают focus на trigger.
