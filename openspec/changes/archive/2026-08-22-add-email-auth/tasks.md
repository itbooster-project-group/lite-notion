## 1. Зависимости и конфигурация окружения

- [x] 1.1 Добавить в `apps/api/package.json` зависимости `@nestjs/jwt`, `@nestjs/passport`, `passport`, `passport-jwt`, `bcrypt`, `cookie-parser`, `@nestjs/schedule` и dev-зависимости `@types/passport-jwt`, `@types/bcrypt`, `@types/cookie-parser`; обновить `pnpm-lock.yaml` через pnpm. Проверка: `pnpm install --frozen-lockfile` проходит из корня, catalog не затронут.
- [x] 1.2 Добавить в `src/config/environment.ts` валидацию `JWT_SECRET` (строка от 32 символов), `ACCESS_TOKEN_TTL_S` (целое 60…3600), `REFRESH_TOKEN_TTL_S` (целое 3600…7776000), `BCRYPT_ROUNDS` (целое 4…15) и валидатор `REFRESH_TOKEN_TTL_S > ACCESS_TOKEN_TTL_S` на свойстве `REFRESH_TOKEN_TTL_S`. Проверка: `environment.spec.ts` покрывает отсутствующий и короткий `JWT_SECRET`, каждую границу числовых диапазонов и противоречивую пару TTL.
- [x] 1.3 Убедиться, что сообщение об ошибке валидации окружения не содержит значение `JWT_SECRET`. Проверка: тест в `environment.spec.ts` подаёт короткий секрет-маркер и утверждает, что текст ошибки его не содержит.
- [x] 1.4 Расширить `ApplicationConfig` и `createApplicationConfig` полями `jwtSecret`, `accessTokenTtlS`, `refreshTokenTtlS`, `bcryptRounds`. Проверка: `application-config.spec.ts` подтверждает проброс всех четырёх значений.
- [x] 1.5 Добавить новые переменные в `apps/api/.env.example` с локальными значениями из design.md (`900`, `2592000`, `12` и секрет-заглушка). Проверка: запуск `pnpm dev:api` со скопированным из шаблона `.env` поднимает API.

## 2. Схема данных

- [x] 2.1 Описать в `apps/api/prisma/schema.prisma` модели `User` и `Session` по design.md, включая `@unique` на `email` и `tokenHash`, индексы по `userId`, `familyId`, `expiresAt` и `onDelete: Cascade`. Проверка: `pnpm --filter @lite-notion/api prisma:generate` завершается успешно и генерирует типы обеих моделей.
- [x] 2.2 Создать первую миграцию через `prisma migrate dev --name add-user-and-session`. Проверка: `prisma migrate status` сообщает «Database schema is up to date», а `prisma migrate diff --from-config-datasource --to-schema --exit-code` — «No difference detected» (миграция полностью воспроизводит datamodel). Применение на отдельной чистой базе не проверялось: создание scratch-базы заблокировано правами окружения.

## 3. Пароли и токены

- [x] 3.1 Реализовать `src/auth/password.service.ts`: хеширование bcrypt с `BCRYPT_ROUNDS` из конфигурации, сравнение и dummy-хеш, посчитанный один раз при инициализации. Проверка: `password.service.spec.ts` подтверждает несовпадение хеша с паролем, успешное и неуспешное сравнение и доступность dummy-хеша для сравнения.
- [x] 3.2 Реализовать `src/auth/token.service.ts`: подпись access JWT с claims `sub` и `sid` и сроком `ACCESS_TOKEN_TTL_S`, генерация криптостойкого refresh-токена и его SHA-256 хеша. Проверка: `token.service.spec.ts` подтверждает наличие обоих claims, срок жизни токена, различие двух подряд сгенерированных refresh-токенов и стабильность хеша.
- [x] 3.3 Реализовать `src/auth/refresh-cookie.ts`: имя cookie, `HttpOnly`, `Path=/api/v1/auth`, `Max-Age`, а также `SameSite` и `Secure`, выводимые из `NODE_ENV`, плюс функцию очистки. Проверка: `refresh-cookie.spec.ts` подтверждает `Lax` без `Secure` в development и `None` с `Secure` в production.

## 4. Сессии, ротация и grace-период

- [x] 4.1 Реализовать `src/auth/session.repository.ts` поверх `PrismaService`: поиск по `tokenHash`, создание сессии, отзыв цепочки по `familyId`, а также ротацию в одной транзакции с `SELECT ... FOR UPDATE` по действующей сессии цепочки. Закрепить инвариант единственной действующей сессии комментарием в коде. Проверка: типы сходятся, `pnpm --filter @lite-notion/api typecheck` проходит.
- [x] 4.2 Реализовать в `src/auth/session.service.ts` выдачу новой сессии (новый `familyId`, `expiresAt` по `REFRESH_TOKEN_TTL_S`, хранение только хеша токена). Проверка: тест подтверждает, что сохранённая запись содержит хеш, а не сам токен, и что срок истечения соответствует настроенному TTL.
- [x] 4.3 Реализовать в `session.service.ts` ротацию: успешный путь, отклонение ненайденного и истёкшего токена, очистка cookie при отказе. Проверка: `session.service.spec.ts` покрывает успешную ротацию, ненайденный токен и истёкшую сессию.
- [x] 4.4 Реализовать grace-период 30 секунд: обновление по сессии, отозванной менее 30 секунд назад, отзывает текущую действующую сессию цепочки и выдаёт новую пару. Проверка: `session.service.spec.ts` с фейковыми таймерами подтверждает успех на границе внутри 30 секунд, а также что после серии обновлений в цепочке остаётся не более одной неотозванной сессии.
- [x] 4.5 Реализовать reuse detection: предъявление сессии, отозванной более 30 секунд назад, отзывает все неотозванные сессии цепочки и отвечает `401`. Проверка: `session.service.spec.ts` подтверждает отказ за границей grace, отзыв цепочки, недействительность самого свежего токена цепочки после отзыва и сохранность сессий других цепочек того же пользователя.
- [x] 4.6 Реализовать отзыв одной сессии и всех сессий пользователя с идемпотентным поведением. Проверка: тест подтверждает, что повторный отзыв уже отозванной сессии не выбрасывает ошибку и что отзыв всех сессий делает недействительными все refresh-токены пользователя.

## 5. Пользователи и сценарии аутентификации

- [x] 5.1 Реализовать `src/users/users.module.ts` и `users.service.ts`: поиск по нормализованному email и по идентификатору, создание пользователя. Проверка: `users.service.spec.ts` подтверждает нормализацию email при поиске и создании.
- [x] 5.2 Реализовать в `src/auth/auth.service.ts` регистрацию: нормализация email, отказ `409` при занятом адресе, сохранение только bcrypt-хеша, открытие сессии. Проверка: `auth.service.spec.ts` покрывает успешную регистрацию, конфликт email и конфликт при отличии только в регистре.
- [x] 5.3 Реализовать вход с неразличимым ответом: одинаковые статус и сообщение для несуществующего email и неверного пароля, вызов `bcrypt.compare` против dummy-хеша при отсутствии пользователя. Проверка: `auth.service.spec.ts` сверяет идентичность обеих ошибок и утверждает, что сравнение пароля вызывается и при ненайденном пользователе.
- [x] 5.4 Реализовать выход с текущего устройства и выход со всех устройств поверх сервиса сессий. Проверка: `auth.service.spec.ts` подтверждает отзыв сессии запроса и всех сессий пользователя.

## 6. Guard и защита маршрутов

- [x] 6.1 Реализовать `public.decorator.ts`, `current-user.decorator.ts` и `jwt.strategy.ts` с разбором claims `sub` и `sid` без обращения к базе. Проверка: типы сходятся, стратегия возвращает `{ id, sessionId }` из полезной нагрузки токена.
- [x] 6.2 Реализовать `jwt-auth.guard.ts` с чтением метаданных `@Public()` через `Reflector.getAllAndOverride` и зарегистрировать его как `APP_GUARD` в `AuthModule`. Проверка: `jwt-auth.guard.spec.ts` покрывает публичный маршрут без токена, приватный без токена, просроченный токен и подделанную подпись, а также утверждает, что при успешной проверке `PrismaService` не вызывается.
- [x] 6.3 Пометить `GET /api/v1/health` декоратором `@Public()`. Проверка: существующие тесты health продолжают проходить, а HTTP-тест подтверждает `200` без заголовка `Authorization`.
- [x] 6.4 Добавить HTTP-тест через `supertest`, подтверждающий, что произвольный приватный маршрут без токена отвечает `401` в едином формате ошибок и не выполняет обработчик.

## 7. HTTP-слой и подключение модуля

- [x] 7.1 Реализовать DTO в `src/auth/dto/`: `RegisterDto` (email, name, пароль 8…72), `LoginDto`, `AuthResponseDto`, `UserProfileDto`, с нормализацией email трансформацией и Swagger-декораторами. Проверка: HTTP-тест подтверждает `400` на некорректном email, коротком пароле и лишнем поле.
- [x] 7.2 Реализовать `auth.controller.ts` с маршрутами `register`, `login`, `refresh`, `logout`, выходом со всех устройств и `me`; контроллер только вызывает сервисы и управляет cookie. Проверка: `auth.controller.spec.ts` подтверждает установку и очистку cookie и отсутствие бизнес-логики в обработчиках.
- [x] 7.3 Убедиться, что ни один ответ не содержит пароль, его хеш, refresh-токен или его хеш. Проверка: тест проходит по телам ответов `register`, `login`, `refresh` и `me` и утверждает отсутствие соответствующих полей.
- [x] 7.4 Зарегистрировать `AuthModule`, `UsersModule` и `ScheduleModule.forRoot()` в `app.module.ts`. Проверка: `pnpm --filter @lite-notion/api build` проходит, приложение поднимается при недоступной базе, как раньше.
- [x] 7.5 Включить `credentials: true` в CORS и подключить `cookie-parser` в `configureApplication`. Проверка: `application.spec.ts` подтверждает `Access-Control-Allow-Credentials: true` для разрешённого origin и отсутствие CORS-разрешения для чужого origin.

## 8. Плановая очистка сессий

- [x] 8.1 Реализовать `session-cleanup.service.ts` с `@Cron(CronExpression.EVERY_HOUR)`, удаляющим истёкшие сессии и сессии, отозванные более 7 дней назад. Проверка: `session-cleanup.service.spec.ts` подтверждает удаление истёкших и давно отозванных и сохранение действующих.
- [x] 8.2 Обернуть выполнение задачи в обработку ошибок с логированием. Проверка: тест подтверждает, что ошибка базы логируется, не пробрасывается наружу и не мешает следующему запуску.

## 9. Документация контракта и финальная проверка

- [x] 9.1 Добавить bearer security scheme в `createOpenApiDocument`, пометить защищённые операции и задать явные operation ID и ответ `401` для всех маршрутов аутентификации. Проверка: сгенерированный OpenAPI JSON содержит схему безопасности и документированный `401` у защищённых операций.
- [x] 9.2 Выполнить `pnpm api:generate` из корня и закоммитить `apps/api/openapi.json` вместе со сгенерированным выводом в `apps/web`. Проверка: повторный запуск команды не создаёт нового diff.
- [x] 9.3 Обновить `apps/api/AGENTS.md`: bootstrap-контракт с четырьмя новыми переменными, разрешённые cross-origin credentials и упоминание, что аутентификация больше не отсутствует. Проверка: описание соответствует фактическому поведению кода.
- [x] 9.4 Прогнать из корня `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. Проверка: все четыре команды завершаются успешно.
- [x] 9.5 Выполнить `openspec validate add-email-auth --strict`. Проверка: команда завершается без ошибок.
