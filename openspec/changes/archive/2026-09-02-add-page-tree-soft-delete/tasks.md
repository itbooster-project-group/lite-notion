## 1. Схема данных и миграция

- [x] 1.1 Добавить в `apps/api/prisma/schema.prisma` enum `PageDeletionOrigin` (`SELF`, `PARENT_PAGE`, `PROJECT`), поле `Page.deletedOrigin PageDeletionOrigin?` и поле `Project.deletedAt DateTime?` с doc-комментариями о парности `deletedAt`/`deletedOrigin`; проверка — `pnpm --filter @lite-notion/api prisma:generate` проходит и типы содержат новые поля
- [x] 1.2 Добавить индексы `Page @@index([deletedAt])`, `Project @@index([ownerId, deletedAt])`, `Project @@index([deletedAt])`; проверка — они присутствуют в сгенерированной миграции
- [x] 1.3 Создать миграцию `prisma migrate dev` и дописать в её SQL CHECK-constraint `("deletedAt" IS NULL) = ("deletedOrigin" IS NULL)` для `Page`; проверка — `prisma migrate deploy` на чистой базе проходит, а ручной `UPDATE "Page" SET "deletedAt" = now()` без `deletedOrigin` отклоняется базой
- [x] 1.4 Добавить в `apps/api/src/common/constants.ts` константу `TRASH_RETENTION_MS` (30 дней) с комментарием о том, что срок не настраивается через окружение; проверка — `pnpm --filter @lite-notion/api typecheck`

## 2. Репозиторий проектов

- [x] 2.1 Ввести отдельный тип `DeletedProjectRecord` с непустым `deletedAt` (а не добавлять nullable-поле в `ProjectRecord`: живой проект его никогда не несёт) и отфильтровать `deletedAt: null` в `findAllByOwner` и `findByIdForOwner` `PrismaProjectsRepository`; проверка — существующие тесты `projects.service.spec.ts` и `projects.http.spec.ts` проходят без изменения ожиданий
- [x] 2.2 Добавить в `ProjectsRepository` методы `softDelete(id, ownerId)`, `restore(id, ownerId)` и `findDeletedByOwner(ownerId)` и реализовать их в `PrismaProjectsRepository`: удаление и восстановление — одна транзакция, `updateMany` по проекту плюс `updateMany` по его страницам (`deletedAt: null` → `PROJECT` при удалении, `deletedOrigin: PROJECT` → `null` при восстановлении); проверка — юнит-тесты репозитория на in-memory реализации из 2.3
- [x] 2.3 Воспроизвести новое поведение в `InMemoryProjectsRepository`, включая доступ к общему хранилищу страниц для каскада; проверка — один и тот же набор сценариев сервиса проходит на in-memory реализации
- [x] 2.4 Добавить интеграционный тест на живой базе (`*.integration-spec.ts`) на каскад удаления и восстановления проекта, включая сохранение источника и отметки времени у страницы, удалённой раньше и самостоятельно; проверка — `pnpm --filter @lite-notion/api test:integration`

## 3. Репозиторий страниц: каскад по поддереву

- [x] 3.1 Расширить `PageRecord` полями `deletedAt` и `deletedOrigin` только там, где они нужны корзине (отдельный тип записи удалённой страницы), не публикуя их в обычном `PageRecord`; проверка — `pnpm --filter @lite-notion/api typecheck`
- [x] 3.2 Реализовать `softDelete(pageId, ownerId)` в `PrismaPagesRepository`: транзакция, `pg_advisory_xact_lock(hashtext(ownerId))` первым действием, рекурсивный CTE из design.md разделом 3, единая отметка времени на всё поддерево, `SELF` корню и `PARENT_PAGE` потомкам; проверка — юнит-тесты сервиса на in-memory реализации и интеграционный тест из 3.7
- [x] 3.3 Реализовать `restore(pageId, ownerId)` в `PrismaPagesRepository`: тот же lock владельца, отказ только при удалённом проекте (источник удаления право на восстановление не гоняет), рекурсивный CTE со спуском только по `PARENT_PAGE`; проверка — сценарии восстановления из `specs/page-tree/spec.md` проходят
- [x] 3.4 Дополнить `restore` подъёмом в корень: при удалённом родителе взять блокировку уровня `siblingLevelLockKey(projectId, null)`, вычислить `positionBetween(последний_root, null)` и в том же `UPDATE` проставить корню `parentPageId = NULL` и новый ранг; проверка — сценарии «Восстановление под удалённым родителем поднимает страницу в корень» и «Поднятая в корень страница встаёт последней»
- [x] 3.5 Реализовать `findDeletedByOwner(ownerId)`: плоский список всех удалённых страниц владельца без исключений по источнику, с отметкой времени удаления и проектом; проверка — юнит-тест на состав выдачи и детерминированность порядка
- [x] 3.6 Воспроизвести все три метода в `InMemoryPagesRepository`, включая подъём в корень с пересчётом ранга, сохранив её роль эталона наблюдаемого контракта; проверка — тесты сервиса, написанные один раз, проходят на in-memory реализации
- [x] 3.7 Добавить интеграционные тесты на живой базе: каскад по поддереву глубиной не меньше трёх уровней, неперемечивание ранее удалённого поддерева, остановка восстановления на `SELF`, подъём в корень при удалённом родителе с сохранением поддерева и рангов соседей, отказ `409` при удалённом проекте; проверка — `pnpm --filter @lite-notion/api test:integration`

## 4. Доменные ошибки и сервисы

- [x] 4.1 Добавить в `apps/api/src/pages/errors.ts` ошибку `PageRestoreProjectDeletedError` с doc-комментарием о том, почему это `409`, а не `404`, и почему ни удалённый родитель, ни источник удаления ошибкой не являются; проверка — `pnpm --filter @lite-notion/api typecheck`
- [x] 4.2 Расширить `toHttpException` в `apps/api/src/pages/helpers.ts` переводом новой ошибки в `ConflictException`; проверка — юнит-тесты покрывают новую ветвь
- [x] 4.3 Добавить в `PagesService` методы `softDelete`, `restore` и `findDeletedTree`, вынеся общий проход сборки дерева из `findTree` в приватный helper, параметризованный правилом вложенности (в живом дереве — `parentPageId`, в корзине — «удалена не самостоятельно и родитель тоже удалён») и сортировкой корней (`deletedAt` по убыванию для корзины, ранг для живого дерева); проверка — `pages.service.spec.ts` покрывает обе выдачи, порядок корней и порядок братьев
- [x] 4.4 Добавить в `ProjectsService` методы `softDelete`, `restore` и `listDeletedForOwner`, сохранив `requireOwned` единственной точкой проверки существования проекта для других модулей; проверка — `projects.service.spec.ts`

## 5. HTTP-контракт страниц

- [x] 5.1 Добавить `DeletedPageDto` и `DeletedPageTreeNodeDto` в `apps/api/src/pages/dto/` с явным перечислением полей и полем `deletedAt`; проверка — `pnpm --filter @lite-notion/api typecheck`
- [x] 5.2 Добавить в `PagesController` маршрут `GET /pages/trash` **выше** `GET /pages/:pageId` с комментарием о значимости порядка; проверка — HTTP-тест `GET /api/v1/pages/trash` отвечает `200`, а не `400` от `ParseUUIDPipe`
- [x] 5.3 Добавить `DELETE /pages/:pageId` с `@HttpCode(204)` и Swagger-декораторами (`operationId`, `204`, `401`, `404`); проверка — `pages.controller.spec.ts` и HTTP-тест на `204` с пустым телом
- [x] 5.4 Добавить `POST /pages/:pageId/restore` с `@HttpCode(200)` и Swagger-декораторами, включая `409`; проверка — HTTP-тесты на `200` (в том числе с подъёмом в корень из-под удалённого и из-под каскадно удалённого предка), `404` и `409`
- [x] 5.5 Покрыть HTTP-тестами изоляцию: удаление, восстановление и корзина чужих страниц отвечают тем же `404` и тем же телом, что и несуществующие; проверка — `access-isolation.http.spec.ts` дополнен новыми маршрутами

## 6. HTTP-контракт проектов

- [x] 6.1 Добавить `DeletedProjectDto` с полем `deletedAt`; проверка — `pnpm --filter @lite-notion/api typecheck`
- [x] 6.2 Добавить в `ProjectsController` маршрут `GET /projects/trash` выше любых параметрических маршрутов, `DELETE /projects/:projectId` с `@HttpCode(204)` и `POST /projects/:projectId/restore`, все со Swagger-декораторами; проверка — `projects.controller.spec.ts`
- [x] 6.3 Покрыть HTTP-тестами удаление проекта со страницами, восстановление и неразличимость удалённого и несуществующего проекта, включая `404` при создании страницы в удалённом проекте; проверка — `projects.http.spec.ts` и `pages.http.spec.ts`

## 7. Ручное окончательное удаление

- [x] 7.1 Добавить в `PagesRepository` метод `purge(pageId, ownerId, cascade)` и реализовать его в `PrismaPagesRepository`: транзакция, advisory lock владельца, рекурсивный CTE по физическому поддереву, сбор заголовков `SELF`-потомков и физический `delete` — в той же транзакции, что и сбор; проверка — интеграционный тест из 7.8
- [x] 7.2 Реализовать отказ подтверждения у страниц: при непустом наборе `SELF`-потомков и `cascade = false` не удалять ничего и вернуть их заголовки вызывающему; проверка — сценарии «Отказ без подтверждения», «Удаление с подтверждением» и «Поддерево без отдельно удалённых страниц не требует подтверждения»
- [x] 7.3 Добавить в `PagesRepository` метод `purgeTrash(ownerId)`: физическое удаление всех удалённых страниц владельца; подтверждения не требует — всё уничтожаемое корзина показывала; проверка — юнит-тесты сервиса
- [x] 7.4 Добавить в `ProjectsRepository` методы `purge(projectId, ownerId, cascade)` и `purgeTrash(ownerId, cascade)`: тот же сбор заголовков страниц с источником, отличным от `PROJECT`, тот же отказ и тот же физический `delete` с опорой на FK-каскады; проверка — юнит-тесты сервиса
- [x] 7.5 Воспроизвести оба набора методов в in-memory реализациях, включая сбор заголовков обречённых записей и отказ без подтверждения; проверка — тесты сервисов проходят на in-memory реализациях
- [x] 7.6 Добавить доменную ошибку `PurgeConfirmationRequiredError`, несущую заголовки обречённых записей, и перевод её в `ConflictException`, кладущий в `message` массив «причина, затем заголовки», плюс методы окончательного удаления и очистки в `PagesService` и `ProjectsService`; проверка — `pages.service.spec.ts`, `projects.service.spec.ts` и юнит-тест перевода ошибки, включая страницу с пустым заголовком
- [x] 7.7 Добавить маршруты: в `PagesController` — `DELETE /pages/trash` (**выше** `DELETE /pages/:pageId`) и `DELETE /pages/trash/:pageId`; в `ProjectsController` — `DELETE /projects/trash` и `DELETE /projects/trash/:projectId`; адресные удаления и очистка корзины проектов принимают `?cascade=true` и документируют `409`; все с `@HttpCode(204)` и Swagger-декораторами; проверка — HTTP-тест на `DELETE /api/v1/pages/trash` отвечает `204`, а не `400` от `ParseUUIDPipe`, и `projects.controller.spec.ts`
- [x] 7.8 Добавить интеграционные тесты на живой базе: исчезновение поддерева и документов, `409` с заголовками страниц без `cascade` и полное удаление с ним, адресное удаление вложенной ветки без ущерба остальной корзине, удаление страницы удалённого проекта с сохранением самого проекта в корзине, `404` на живой, очистка корзины страниц оставляет удалённый проект восстановимым, но пустым, идемпотентность очистки пустой корзины; проверка — `pnpm --filter @lite-notion/api test:integration`
- [x] 7.9 Покрыть HTTP-тестами подтверждение у проектов: `409` с заголовками страниц в `message` без параметра, `204` с параметром, отсутствие `409` у проекта без отдельно удалённых страниц, то же для очистки корзины проектов; проверка — `projects.http.spec.ts`
- [x] 7.10 Покрыть HTTP-тестами изоляцию: окончательное удаление и очистка чужой корзины отвечают `404` тем же телом и не трогают чужие записи; проверка — `access-isolation.http.spec.ts`

## 8а. Восстановление в другой проект

- [x] 8а.1 Добавить `RestorePageDto` с необязательным `projectId` (UUID) и доменную ошибку `PageRestoreTargetProjectRejectedError` (`400`) для случая, когда проект назначения указан, а собственный проект страницы жив; проверка — `pnpm --filter @lite-notion/api typecheck` и юнит-тест перевода ошибки в `BadRequestException`
- [x] 8а.2 Расширить `restore` в `PagesRepository` до `(id, ownerId, targetProjectId)`: проверка живости собственного проекта, проверка проекта назначения через `deletedAt: null` и владельца, отказ `409` без него и `400` при ненужном; проверка — сценарии восстановления из `specs/page-tree/spec.md`
- [x] 8а.3 Реализовать перенос в `PrismaPagesRepository`: отдельный statement с рекурсивным CTE по **всему физическому** поддереву, меняющий `projectId`, до statement'а снятия отметки; спуск снятия отметки поменять с `= 'PARENT_PAGE'` на `<> 'SELF'`; проверка — интеграционный тест из 8а.6
- [x] 8а.4 Воспроизвести поведение в `InMemoryPagesRepository`, включая перенос всего физического поддерева и новый ранг в проекте назначения; проверка — тесты сервиса проходят на in-memory реализации
- [x] 8а.5 Принять тело в `POST /pages/:pageId/restore` со Swagger-декораторами и документированным `400`; проверка — HTTP-тесты на `200` с переносом, `400`, `404` и `409`
- [x] 8а.6 Добавить интеграционный тест на живой базе: перенос поддерева одним statement'ом проходит тройной FK, удалённая вложенная ветка едет за переносимой страницей и остаётся удалённой, ранги root-страниц назначения не меняются; проверка — `pnpm --filter @lite-notion/api test:integration`

## 8б. Сериализация создания с мягким удалением

- [x] 8б.1 Взять в `PrismaPagesRepository.create` блокировку владельца первой, до уровневой, и повторить проверки живости проекта и родителя внутри транзакции; проверка — интеграционный тест из 8б.3
- [x] 8б.2 Взять ту же блокировку во всех операциях `PrismaProjectsRepository`: в `softDelete` и `restore` — чтобы создание не проскакивало мимо удаления проекта, в `purge` и `purgeTrash` — чтобы подсчёт обречённых страниц не разъезжался с удалением; проверка — тот же тест
- [x] 8б.3 Добавить `create-under-deleted.integration-spec.ts`: удержанная блокировка владельца останавливает `create` и окончательное удаление проекта на входе, а после её отпускания они видят чужую пометку и отвергают запись; отдельными запросами проверить, что живой страницы под удалённым предком не осталось и что страница, помеченная в гонке, не уехала мимо подтверждения; проверка — снятие любой из двух блокировок роняет соответствующие тесты

## 8. Отложенная очистка по сроку хранения

- [x] 8.1 Создать `apps/api/src/trash/trash-cleanup.repository.ts` с методом `purgeExpired(cutoff)`: `deleteMany` по `Project`, затем по `Page`, с опорой на физические FK-каскады; проверка — интеграционный тест на живой базе, что документ и поддерево исчезают вместе с корнем
- [x] 8.2 Создать `apps/api/src/trash/trash-cleanup.service.ts` с `@Cron(CronExpression.EVERY_DAY_AT_3AM)` по образцу `SessionCleanupService`: логирование, отказ базы не роняет процесс; проверка — `trash-cleanup.service.spec.ts` покрывает успешный запуск, пустой запуск и отказ репозитория
- [x] 8.3 Создать `apps/api/src/trash/trash-cleanup.module.ts` (зависит только от `DatabaseModule`) и подключить его в `AppModule`; проверка — `application.spec.ts` поднимает приложение с новым модулем
- [x] 8.4 Добавить тест, что очистка не трогает записи в пределах срока хранения и восстановленные записи; проверка — юнит-тест с подменённым временем
- [x] 8.5 Добавить интеграционный тест на инвариант отметок времени: потомок никогда не помечен позже предка, поэтому физический каскад не уносит непросроченные записи; покрыть и случай «потомок удалён самостоятельно раньше предка», и случай «восстановленная страница удалена заново», где она уже выведена из прежнего поддерева; проверка — `pnpm --filter @lite-notion/api test:integration`

## 9. Генерация контракта и финальные проверки

- [x] 9.1 Выполнить `pnpm api:generate`; сгенерированные `apps/api/openapi.json` и `apps/web/src/shared/api/generated` лежат в рабочем дереве и стабильны (повторный запуск не даёт нового diff). Коммит — за автором изменения; `pnpm api:check` проходит после него
- [x] 9.2 Прогнать `pnpm lint`, `pnpm typecheck`, `pnpm test` из корня; проверка — все три команды завершаются успешно
- [x] 9.3 Прогнать `pnpm --filter @lite-notion/api test:integration` на поднятой через `pnpm db:up` базе; проверка — все интеграционные тесты проходят
- [x] 9.4 Выполнить `openspec validate add-page-tree-soft-delete --strict`; проверка — валидация проходит без ошибок
