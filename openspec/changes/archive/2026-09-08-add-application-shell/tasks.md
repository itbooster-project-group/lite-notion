## 1. Зависимости и UI-основа

- [x] 1.1 Добавить `zustand` как прямую зависимость `@lite-notion/web`, обновить `pnpm-lock.yaml` с Node.js 22.13+ и pnpm 11.21.0 и проверить установку командой `pnpm install --frozen-lockfile`.
- [x] 1.2 Добавить или адаптировать доступный modal/dialog primitive через публичный API `shared/ui` и проверить отдельным компонентным тестом открытие, Escape, focus trap и возврат фокуса.

## 2. Модель состояния каркаса

- [x] 2.1 Реализовать Zustand store для `desktopCollapsed` и `mobileOpen` с раздельными actions и partial persistence только desktop-предпочтения; проверить unit-тестами начальное состояние и независимость desktop/mobile transitions.
- [x] 2.2 Добавить безопасную rehydration и fallback-обработку недоступного или повреждённого `localStorage`; проверить тестами восстановление сохранённого boolean, отсутствие persistence для `mobileOpen` и работоспособность store при storage-ошибке.

## 3. Виджет AppShell

- [x] 3.1 Создать публичный FSD-виджет `widgets/app-shell` с типизированными слотами `user`, `actions`, `pageTree` и `children`; проверить компонентным тестом правильное размещение переданных слотов и устойчивость при отсутствии каждого опционального слота.
- [x] 3.2 Реализовать desktop-layout и переключение развёрнутого/свёрнутого сайдбара с согласованным hydration-состоянием; проверить тестом состояния, доступные названия, `aria-expanded`, `aria-controls` и возможность обратного переключения.
- [x] 3.3 Реализовать mobile-trigger и модальный сайдбар, не изменяющие desktop-предпочтение; проверить компонентными тестами открытие, закрытие кнопкой/Escape/подложкой, focus lifecycle и независимость persisted desktop state.
- [x] 3.4 Добавить закрытие временного mobile-overlay после завершения продуктовой навигации и при переходе в широкий режим; проверить тестами обоих переходов без изменения `desktopCollapsed`.
- [x] 3.5 Оформить семантическую структуру с именованным `aside` и единственным `main`, а также responsive-видимость и sidebar-токены; проверить через доступные RTL queries и контракт CSS-классов для широкого и узкого режимов.

## 4. Интеграция с App Router

- [x] 4.1 Адаптировать существующий PrivateLayout с PrivateRoute и `AppShell`, сохранив глобальный root layout серверным и ограниченным providers; проверить структурным тестом композицию route layout.
- [x] 4.2 Сохранить существующие приватные и публичные маршруты без изменения URL; проверить композицию и отсутствие AppShell на auth-экранах.

- [x] 4.3 Передать существующее дерево в `AppShell.pageTree` через композицию app-слоя и публичные API; удалить отдельные sidebar/mobile-overlay из WorkspacePage. Проверить навигацию, раскрытие, CRUD/перенос, загрузку и ошибки, включая переход на профиль.
- [x] 4.4 Заменить логотип PrivateShell слотом хлебных крошек, подключить текущий маршрут и данные через существующие селекторы; убрать крошки из контента. Проверить корень, проект, вложенную страницу, профиль, переход по предку и недоступные данные.
- [x] 4.5 Устранить вложенные `main` при композиции AppShell и экранов; проверить единственный main на приватных маршрутах, включая загрузку и ошибки.
- [x] 4.6 Добавить `cursor: pointer` доступным кнопкам через shared Button и нативные кнопки затронутого UI; проверить в браузере доступные и disabled/aria-disabled состояния.
- [x] 4.7 Добавить shared tooltip wrapper из установленного UI-стека и подсказки всем элементам только с иконкой в каркасе, шапке и дереве. Проверить hover, клавиатурный фокус, Escape, доступное имя и активацию кнопки; проверить смену текста toggle.

## 5. Итоговая проверка

- [x] 5.1 Запустить `pnpm --filter @lite-notion/web test`, `pnpm --filter @lite-notion/web typecheck`, `pnpm steiger:web` и `pnpm lint`; устранить все ошибки в затронутой области. При изменении зависимостей или общей конфигурации дополнительно выполнить полный root-набор `pnpm typecheck`, `pnpm test`, `pnpm build`.
- [x] 5.2 Запустить `pnpm --filter @lite-notion/web build` и вручную проверить в браузере desktop collapse/reload, mobile overlay, клавиатурное управление и навигацию между продуктовыми маршрутами.
- [x] 5.3 Запустить `openspec validate add-application-shell --strict` и подтвердить, что все артефакты и delta-spec проходят строгую валидацию.
