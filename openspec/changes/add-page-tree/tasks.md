## 1. Схема данных и миграция

- [x] 1.1 Описать в `apps/api/prisma/schema.prisma` модель `Project` по design.md, раздел 2: `id`, `ownerId`, `name`, связь с `User` (`onDelete: Cascade`), `@@unique([id, ownerId])` и `@@index([ownerId])`. Проверка: `pnpm --filter @lite-notion/api prisma:generate` завершается успешно и генерирует тип `Project`.
- [x] 1.2 Описать модель `Page` со всеми полями из design.md, раздел 2, включая `@@unique([id, ownerId, projectId])`, composite FK на `Project` через `(projectId, ownerId)`, self-relation `PageTree` через тройной composite FK `(parentPageId, ownerId, projectId) → Page(id, ownerId, projectId)` с `onDelete: Cascade, onUpdate: NoAction`, связи `PageOwner` (`Cascade`) и `PageCreatedBy` (`Restrict`), индексы `[ownerId, parentPageId, position]` и `[ownerId, deletedAt]`. Проверка: `prisma:generate` проходит и в сгенерированных типах присутствуют `parent`, `children` и `project`.
- [x] 1.3 Описать модель `PageDocument` с `pageId` в роли первичного ключа и внешнего ключа на `Page` (`onDelete: Cascade`), полями `tiptapSchemaVersion`, `yjsState`, `storageRevision`, `createdAt`, `updatedAt`. Проверка: `prisma:generate` проходит, связь `Page.document` типизирована как опциональная «один к одному».
- [x] 1.4 Создать миграцию через `prisma migrate dev --name add-page-tree`. Проверка: `prisma migrate status` сообщает «Database schema is up to date», а `prisma migrate diff --from-config-datasource --to-schema --exit-code` — «No difference detected».
- [x] 1.5 Проверить сгенерированный SQL миграции глазами — автоматического теста на constraints и индексы в этом изменении нет, поэтому ревью миграции остаётся единственной проверкой: FK `Page → Project` ссылается на пару `(id, ownerId)`, self-FK `Page → Page` — на тройку `(id, ownerId, projectId)`, а не на один `id`; nullable `parentPageId` не сопровождается `MATCH FULL`; присутствуют оба unique-индекса, оба индекса `Page` и `ON DELETE` у каждого внешнего ключа согласно design.md, раздел 2. Проверка: соответствующие `CREATE UNIQUE INDEX`, `CREATE INDEX` и `ALTER TABLE ... ADD CONSTRAINT` перечислены в описании Pull Request и подтверждены человеческим ревью. Дополнительно все четыре запрета проверены вручную на живой PostgreSQL 18 27.08.2026: чужой владелец родителя, чужой проект родителя и проект другого пользователя отклонены нарушением FK, root-страница с NULL-родителем прошла, каскад удаления поддерева сработал.

## 2. Модуль проектов

- [x] 2.1 Объявить в `src/projects/projects.repository.ts` интерфейс репозитория и типы записей: создание проекта и чтение проектов владельца; реализовать Prisma-версию и `projects.repository.in-memory.ts`. Проверка: `pnpm --filter @lite-notion/api typecheck` проходит, обе реализации удовлетворяют одному интерфейсу.
- [x] 2.2 Реализовать `src/projects/errors.ts` с `ProjectNotFoundError` без зависимости от HTTP. Проверка: файл не импортирует `@nestjs/common`.
- [x] 2.3 Реализовать `src/projects/projects.service.ts`: создание проекта с владельцем из текущего пользователя, список проектов владельца в детерминированном порядке, разрешение чужого или несуществующего проекта в `ProjectNotFoundError`. Проверка: `projects.service.spec.ts` покрывает создание, два проекта с одинаковым именем, пустой список, отсутствие чужих проектов в выдаче и идентичность ошибки для чужого и несуществующего идентификатора.
- [x] 2.4 Реализовать DTO в `src/projects/dto/`, по одному классу на файл: `CreateProjectDto` (непустое имя с ограничением длины из `src/projects/constants.ts`) и `ProjectDto`. Проверка: HTTP-тест подтверждает `400` на пустом имени, слишком длинном имени и лишнем поле.
- [x] 2.5 Реализовать `src/projects/projects.controller.ts` с маршрутами `POST /api/v1/projects` и `GET /api/v1/projects`; контроллер только извлекает `@CurrentUser()`, вызывает сервис и переводит ошибки домена в HTTP-исключения. Проверка: `projects.controller.spec.ts` подтверждает отсутствие бизнес-логики в обработчиках и вызов соответствующего метода сервиса.
- [x] 2.6 Реализовать `src/projects/projects.module.ts`, экспортировать сервис проектов для `PagesModule` и зарегистрировать модуль в `src/app.module.ts`. Проверка: `PagesModule` резолвит зависимость в тестовом Nest-модуле, `build` проходит.

## 3. Fractional rank

- [x] 3.1 Реализовать в `src/pages/constants.ts` алфавит base62, границы длины ранга (`POSITION_MAX_LENGTH = 255`), максимальную длину `title`, `TIPTAP_SCHEMA_VERSION` и `DOCUMENT_MAX_BYTES`. Проверка: константы импортируются DTO, хелперами и репозиторием, дублирующих литералов в сервисах нет.
- [x] 3.2 Реализовать в `src/pages/helpers.ts` генератор ранга: ранг перед первым элементом, ранг после последнего и ранг строго между двумя соседями. Проверка: `pages/helpers.spec.ts` подтверждает лексикографический порядок результата относительно обоих соседей, вставку между соседними по алфавиту ключами (`a`, `b`), вставку в пустой уровень, вставку в начало и в конец.
- [x] 3.3 Покрыть деградацию длины: серия вставок в одну и ту же щель. Проверка: `pages/helpers.spec.ts` подтверждает, что после ста последовательных вставок между одной парой ранги остаются строго упорядоченными и не превышают `POSITION_MAX_LENGTH`.
- [x] 3.4 Реализовать в `helpers.ts` детерминированное сравнение братьев (`position`, затем `id`). Проверка: тест подтверждает стабильный порядок для двух страниц с одинаковым `position` и разными `id`.

## 4. Репозиторий страниц

- [x] 4.1 Объявить в `src/pages/pages.repository.ts` интерфейс репозитория и типы записей: чтение всех неудалённых страниц владельца, чтение одной неудалённой страницы владельца, чтение рангов братьев, создание страницы с документом, обновление заголовка, перемещение, чтение и замена содержимого документа. Проверка: `typecheck` проходит.
- [x] 4.2 Реализовать в `src/pages/errors.ts` типы `PageNotFoundError`, `PageCycleError`, `SiblingParentMismatchError`, `PageProjectMismatchError` без зависимости от HTTP. Проверка: файл не импортирует `@nestjs/common`.
- [x] 4.3 Реализовать Prisma-реализацию чтения: плоский `SELECT` страниц владельца с фильтром `deletedAt: null` и `ORDER BY parentPageId NULLS FIRST, position, id`; чтение одной страницы по паре `id` + `ownerId` с тем же фильтром. Проверка: тест подтверждает, что страница с проставленным `deletedAt` не попадает ни в дерево, ни в чтение одной страницы.
- [x] 4.4 Реализовать создание страницы: `Page` и `PageDocument` в одной транзакции, `ownerId` и `createdById` из текущего пользователя, `projectId` из запроса либо от родителя, `position` — ранг после последнего брата, документ инициализируется `TIPTAP_SCHEMA_VERSION`, пустым `yjsState` и `storageRevision = 0`. Проверка: тест подтверждает, что обе строки создаются в одном `$transaction`, что документ пустой и что при ошибке создания документа страница не остаётся.
- [x] 4.5 Реализовать перемещение в одной транзакции: первой операцией `pg_advisory_xact_lock` по хешу `ownerId`, затем чтение перемещаемой страницы и нового родителя, проверка совпадения проекта, проверка цикла рекурсивным CTE по предкам нового родителя, чтение рангов соседей и единственный `UPDATE` с новыми `parentPageId` и `position`. Закрепить порядок «сначала lock» комментарием в коде. Проверка: `typecheck` проходит, advisory lock — первый оператор транзакции, `projectId` в `UPDATE` не участвует.
- [x] 4.6 Реализовать `src/pages/pages.repository.in-memory.ts` по образцу `auth.repository.in-memory.ts`, воспроизводящий те же правила владельца, проекта, цикла, порядка, фильтра удалённых и атомарности. Проверка: та же спека сервиса проходит на in-memory реализации без обращения к Prisma.

## 5. Сервис дерева

- [x] 5.1 Реализовать в `src/pages/pages.service.ts` создание root-страницы и дочерней страницы: подстановка владельца и создателя из текущего пользователя, пустой заголовок при отсутствующем, размещение последней среди братьев. Проверка: `pages.service.spec.ts` покрывает создание root, создание ребёнка, наследование владельца и проекта от родителя, создание с пустым заголовком и запись создателя.
- [x] 5.2 Реализовать проверку проекта при создании: проект должен принадлежать текущему пользователю, а при указанном родителе — совпадать с проектом родителя. Проверка: `pages.service.spec.ts` подтверждает `ProjectNotFoundError` для чужого и несуществующего проекта с идентичной ошибкой и `PageProjectMismatchError` при несовпадении с проектом родителя.
- [x] 5.3 Реализовать отказ при родителе из чужого дерева и при несуществующем родителе. Проверка: `pages.service.spec.ts` подтверждает, что обе ситуации дают один и тот же `PageNotFoundError` и что страница не создаётся.
- [x] 5.4 Реализовать сборку дерева из плоского списка за один проход и выдачу дерева только текущего владельца. Проверка: `pages.service.spec.ts` покрывает несколько уровней вложенности, пустое дерево, отсутствие чужих страниц в ответе при наличии другого владельца в хранилище и отсутствие страниц с проставленным `deletedAt`.
- [x] 5.5 Реализовать чтение одной страницы с неразличимым отказом для чужой, удалённой и несуществующей. Проверка: тест сверяет идентичность всех трёх ошибок.
- [x] 5.6 Реализовать переименование: изменение заголовка, неизменность структурных полей. Проверка: `pages.service.spec.ts` подтверждает неизменность `parentPageId`, `position`, `ownerId` и `projectId` после переименования.
- [x] 5.7 Реализовать перемещение: разрешение соседей в новый ранг, перенос в корень при пустом родителе, размещение последним при отсутствии обоих соседей. Проверка: `pages.service.spec.ts` покрывает перенос под другого родителя, перенос в корень с неизменным проектом, перестановку между соседями с неизменными рангами остальных братьев и перенос в конец уровня.
- [x] 5.8 Реализовать отклонение цикла: перенос в саму себя и перенос в потомка на произвольной глубине. Проверка: `pages.service.spec.ts` подтверждает `PageCycleError` в обоих случаях и полную неизменность дерева после отказа.
- [x] 5.9 Реализовать отказ при родителе или соседе из чужого дерева, при родителе из другого проекта и при несогласованном соседе. Проверка: тест подтверждает `PageNotFoundError` для чужого и несуществующего идентификатора, `PageProjectMismatchError` для своего родителя из другого проекта, `SiblingParentMismatchError` для своего соседа под другим родителем и неизменность владельца и проекта перемещаемой страницы.
- [x] 5.10 Покрыть сохранность поддерева и атомарность отката. Проверка: тест подтверждает, что после перемещения родители и относительный порядок потомков не изменились, а при ошибке после смены родителя, но до записи ранга, дерево возвращается к исходному состоянию.

## 6. HTTP-слой страниц

- [x] 6.1 Реализовать DTO в `src/pages/dto/`, по одному классу на файл: `CreatePageDto` (обязательный `projectId`, необязательные `parentPageId` и `title`), `RenamePageDto`, `MovePageDto`, `PageDto`, `PageTreeNodeDto` — с ограничениями длины из `constants.ts`, UUID-валидацией идентификаторов и Swagger-декораторами. Проверка: HTTP-тест подтверждает `400` на отсутствующем `projectId`, не-UUID идентификаторах, слишком длинном заголовке и лишнем поле.
- [x] 6.2 Убедиться, что ни один DTO запроса не принимает `ownerId`, `createdById`, `position` и `deletedAt`, а `projectId` принимается только при создании. Проверка: HTTP-тест подтверждает `400` при передаче каждого из этих полей в создании, переименовании и перемещении.
- [x] 6.3 Реализовать `src/pages/pages.controller.ts` с маршрутами создания, чтения дерева, чтения страницы, переименования и перемещения; контроллер только извлекает `@CurrentUser()`, вызывает сервис и переводит ошибки домена в HTTP-исключения. Проверка: `pages.controller.spec.ts` подтверждает отсутствие бизнес-логики в обработчиках и вызов соответствующего метода сервиса для каждого маршрута.
- [x] 6.4 Задать соответствие ошибок домена и статусов: `PageNotFoundError` и `ProjectNotFoundError` → `404`, `PageCycleError` → `409`, `SiblingParentMismatchError` и `PageProjectMismatchError` → `400`. Проверка: `pages.controller.spec.ts` покрывает все пять переводов.
- [x] 6.5 Реализовать `src/pages/pages.module.ts` и зарегистрировать его в `src/app.module.ts`. Проверка: `pnpm --filter @lite-notion/api build` проходит, приложение поднимается при недоступной базе, `src/generate-openapi.ts` выполняется без базы.

## 7. Содержимое документа

- [x] 7.1 Поднять предел JSON-тела в `configureApplication` (`src/application.ts`) выше `DOCUMENT_MAX_BYTES` с запасом, чтобы отказ по размеру формировал DTO, а не парсер Express. Проверка: `application.spec.ts` подтверждает, что тело чуть меньше предела принимается, а тело заметно больше него отклоняется в едином формате ошибок.
- [x] 7.2 Расширить репозиторий страниц чтением и полной заменой документа по паре `pageId` + `ownerId`: чтение возвращает `tiptapSchemaVersion` и `yjsState`, запись заменяет оба поля и инкрементирует `storageRevision`. Проверка: `typecheck` проходит, обе операции фильтруют `deletedAt: null` так же, как чтение страницы.
- [x] 7.3 Реализовать `src/pages/page-document.service.ts`: чтение содержимого и его полная замена, разрешение чужой, удалённой и несуществующей страницы в тот же `PageNotFoundError`, что и у сервиса дерева. Проверка: `page-document.service.spec.ts` подтверждает чтение пустого документа только что созданной страницы, возврат последнего записанного state, замену при повторной записи и идентичность ошибок для чужой и несуществующей страницы.
- [x] 7.4 Подтвердить, что сервис не разбирает содержимое: запись произвольных байтов и их чтение возвращают ровно те же байты. Проверка: тест записывает случайный буфер, читает его обратно и сравнивает побайтно, не создавая зависимости от `yjs`.
- [x] 7.5 Реализовать DTO в `src/pages/dto/`: `PageDocumentDto` и `UpdatePageDocumentDto` (`tiptapSchemaVersion` — целое, `yjsState` — base64 не длиннее `DOCUMENT_MAX_BYTES`), с Swagger-декораторами и форматом base64. Проверка: HTTP-тест подтверждает `400` на не-base64 строке, на отсутствующей версии схемы, на превышении предела и на лишнем поле.
- [x] 7.6 Реализовать маршруты `GET /api/v1/pages/{pageId}/document` и `PUT /api/v1/pages/{pageId}/document` в контроллере документа; контроллер только извлекает `@CurrentUser()`, вызывает сервис и переводит ошибки домена в HTTP-исключения. Проверка: `page-document.controller.spec.ts` подтверждает отсутствие бизнес-логики и вызов сервиса для обоих маршрутов.
- [x] 7.7 Подтвердить отсутствие отдельных маршрутов создания и удаления документа. Проверка: тест перебирает зарегистрированные маршруты и утверждает, что среди них нет `POST` и `DELETE` для пути документа, а сгенерированный `openapi.json` их не содержит.
- [x] 7.8 Подтвердить, что запись содержимого не затрагивает дерево. Проверка: тест сравнивает заголовок, родителя, ранг, владельца и проект страницы до и после записи документа.

## 8. Изоляция владельца и защита маршрутов

- [x] 8.1 Добавить HTTP-тест на неразличимость: чужая и несуществующая страница дают одинаковые статус и тело для чтения, переименования, перемещения, чтения документа и записи документа; чужой и несуществующий проект дают одинаковые статус и тело при создании страницы. Проверка: тест сравнивает статус и тело попарно для каждой операции.
- [x] 8.2 Добавить HTTP-тест на отсутствие `403`: ни одна операция над идентификатором из чужого дерева или чужим проектом не возвращает этот статус. Проверка: тест перебирает все маршруты страниц, документов и проектов и утверждает, что статус ответа не равен `403`.
- [x] 8.3 Добавить HTTP-тест на закрытость маршрутов: каждый маршрут страниц, документов и проектов без заголовка `Authorization` отвечает `401` в едином формате ошибок и не выполняет обработчик. Проверка: тест перебирает маршруты и подтверждает, что сервис не вызывался.
- [x] 8.4 Убедиться, что ни один маршрут страниц, документов и проектов не помечен `@Public()`. Проверка: тест читает метаданные всех трёх контроллеров и утверждает отсутствие публичного маркера на классе и на каждом обработчике.

## 9. OpenAPI и сгенерированный клиент

- [x] 9.1 Проставить каждой операции страниц, документов и проектов явный стабильный operation ID и Swagger-описания ответов `200`/`201`, `400`, `401`, для страниц и документов дополнительно `404`, а перемещению — `409`; описать `yjsState` как строку формата base64; пометить операции bearer-схемой безопасности. Проверка: сгенерированный `openapi.json` содержит все перечисленные ответы и security для каждой операции.
- [x] 9.2 Регенерировать артефакты командой `pnpm api:generate` из корня и закоммитить `apps/api/openapi.json` вместе со сгенерированным выводом в `apps/web/src/shared/api/generated`. Проверка: генерация детерминирована — повторный `pnpm api:generate` поверх сгенерированного вывода не даёт diff; `pnpm api:check` пройдёт в CI после коммита артефактов.

## 10. Финальная проверка

- [x] 10.1 Прогнать проверки затронутого scope: `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` из корня. Проверка: все четыре команды завершаются успешно.
- [x] 10.2 Выполнить `openspec validate add-page-tree --strict`. Проверка: команда завершается без ошибок.
