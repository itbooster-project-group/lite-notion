import { ProfileView } from '@/widgets/profile-view';

export function ProfilePage() {
  return (
    <main className="mx-auto max-w-shell px-page-inline py-page-block-compact">
      <ProfileView />
    </main>
  );
}
